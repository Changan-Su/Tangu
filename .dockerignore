# Tangu-Agent 构建上下文裁剪（context = apps/Tangu-Agent/）
node_modules
dist
desktop
archived
# 插件 drop-in 目录（worker 等）：运行时加载，不烘焙进核心镜像。
.git
# 嵌套插件仓（forsion-worker 独立成仓）的 git 历史别烘进镜像（裸 .git 只匹配根级）
plugins/*/.git
*.log
.env
.env.*

#designs
designs/