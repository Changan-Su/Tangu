# Dependencies
node_modules/

#designs
designs/

# Build output
dist/
out/
desktop/out/
desktop/dist/
# 打包前 beforeBuild 下载的内置 Python(体积大、按平台/arch 拉取,不入库)
desktop/build/python/
# postinstall 从 node_modules 拷出的 excalidraw 自托管字体(13M,可再生,不入库)
desktop/frontend/public/excalidraw/

# TypeScript
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# 插件 drop-in 目录：运行时从 ./plugins 发现加载（见 src/tangu-agent/plugins/loader.ts）。
# worker 等插件独立维护/分发，永不提交进核心仓库（开源核心 = standalone/desktop/TUI）。
/tangu-agent/plugins/

Dockerfile
docker-compose.yml